# All production deps EXCEPT heavy CV libraries (face_recognition, opencv).
# Those are mocked at the conftest level so dlib never needs to be compiled in CI.
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<4.0.0

numpy==1.26.4
Pillow==10.3.0

python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
aiofiles==23.2.1

# Test-only
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
